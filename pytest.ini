[pytest]
# settings_test is config.settings with every outbound seam (mail, Sentry, S3, FCM) blanked,
# so a developer's .env cannot make the suite reach a provider. See that module's docstring.
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
